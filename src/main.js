const lists = document.querySelector('.b_ex')
const option = document.querySelectorAll('.options')

lists.addEventListener('click',(e)=>{
    const obj = e.target.className
    const o = option.map(
        
    )

})